
// import express, { NextFunction } from "express";
// import { UserController } from "../controllers/UserController";
// import { UserInteractor } from "../interactors/UserInteractor";


// const userInteractor = new UserInteractor();
// const userController = new UserController(userInteractor);
// const router = express.Router();
// import {userRoutes} from "./updatedUserRoutes"
// const userRouter = new userRoutes()
// router.post("/create-user", userController.onCreateUser.bind(userController));
// router.post("/fetch-credits-left", userController.onFetchCreditsLeft.bind(userController));
// router.post("/increase-credits", userController.onIncreaseCredits.bind(userController));
// router.post("/get-user", userController.onGetUser.bind(userController));
// router.post("/sign-in", userController.onSignIn.bind(userController));
// router.post("/test",userRoutes.)


// export default router;
